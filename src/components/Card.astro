---
// export interface Props {
// 	title: string;
// 	body: string;
// 	href: string;
// }

// const { href, title, body } = Astro.props;
const {session} = Astro.props;
---

<script>
	import '@lifelongdev/dwane-timer/dwane-timer.js';

</script>
<li class="card">
	<div class="card-preview">
		<img src={session.sessionImageURL} />
		<!-- <video autoplay muted loop src="/assets/videos/xr-tictactoe-demo.mp4"></video> -->
	</div>
	<div class="card-info">
		<h1 class="card-title">{session.title}</h1>
		<dwane-timer timer-deadline={session.timeInMs} />
		<br/><p class="card-title"><a href={session.slug}>enter</a></p>
	</div>
</li>
<style>
    .card {
        display: flex;
        flex-direction: column;
        justify-content: space-between;
        /* padding: 1rem; */
        border: 1px solid black;
        width: 100%;
        max-width: 500px;
        border-radius: 20px;
        /* border-radius: 20px 20px 0 0; */
        overflow: hidden;

    }

    .card-preview {
        /* height: 300px; */
        width: 100%;
        /* max-width: 300px; */
        position: relative;
        overflow: hidden;
        /* margin-bottom: 10px; */
    }

    .card-preview img {
		width: 100%;
        /* position: absolute;
        left: 50%;
        top: 50%;
        -webkit-transform: translate(-50%,-50%);
        -moz-transform: translate(-50%,-50%);
        -ms-transform: translate(-50%,-50%);
        transform: translate(-50%,-50%);         */
    }

    .card-info {
        /* position: absolute;
        left: 50%;
        top: 50%;
        -webkit-transform: translate(-50%,-50%);
        -moz-transform: translate(-50%,-50%);
        -ms-transform: translate(-50%,-50%);
        transform: translate(-50%,-50%); */
        text-align: center;
    }

    .card-title {
        /* background-color: rgba(0, 0, 0, .7);
        color: white;  
        padding: 5px;
        margin-bottom: 15px; */
		font-size: clamp(.875rem,.45rem + 2vw,2.125rem);
    }

    details {
        margin: 5px;
        cursor: pointer;
        text-align: center;
    }
    summary {
        /* text-align: center; */
    }


</style>